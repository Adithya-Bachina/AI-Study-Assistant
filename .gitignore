# Python specific ignores
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
env/
venv/
*env/                 # Catches virtual environments named like 'my_env'
*.egg-info/
.mypy_cache/
.pytest_cache/
.ipynb_checkpoints/   # Jupyter/Colab notebook checkpoints
.vscode/              # VS Code specific settings (if you use VS Code later)

# Operating System files
.DS_Store             # macOS
Thumbs.db             # Windows

# Data and models (usually not committed directly to Git, especially if large)
/data/                # Raw or processed data files
/models/              # Trained models or intermediate model artifacts

# Flask specific ignores (for later)
instance/
.flaskenv
